https://codeforces.com/problemset/problem/61/A

a = input().strip()
b = input().strip()

# Perform bitwise XOR using string comprehension
result = ''.join(['1' if x != y else '0' for x, y in zip(a, b)])

# Print the result
print(result)
