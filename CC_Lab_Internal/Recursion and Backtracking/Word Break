Given a string and a dictionary, determine if it can be segmented into valid words.
→ Recursion + memoization / DP

class Solution:
    def wordBreak(self, s, wordDict):
        w, dp = set(wordDict), [True] + [False]*len(s)
        for i in range(1, len(s)+1):
            for j in range(i):
                if dp[j] and s[j:i] in w:
                    dp[i] = True
                    break
        return dp[-1]

print(Solution().wordBreak("leetcode", ["leet","code"]))
