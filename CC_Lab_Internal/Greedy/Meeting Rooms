Given intervals of meeting times [start, end], determine if a person can attend all meetings (no overlap).
→ Return True/False.

class Solution:
    def canAttendMeetings(self, intervals):
        intervals.sort()
        return all(intervals[i][0] >= intervals[i-1][1] for i in range(1, len(intervals)))

print(Solution().canAttendMeetings([[0,30],[5,10],[15,20]]))
